var ValidationRule = require("../validationRule");

module.exports = class HasValueValidator extends ValidationRule
{
    constructor(value)
    {
        super("hasValue", value);
    }

    validate()
    {
        var isValid = this.value != null;
        if(isValid) return;
        throw `Does not have a value.`
    }

}
