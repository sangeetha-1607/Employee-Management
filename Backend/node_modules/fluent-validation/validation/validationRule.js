module.exports = class ValidationRule
{
    constructor(ruleName, value)
    {
        if(ruleName == null) throw "The rule has to have a name."
        this.value = value;
        this.ruleName = ruleName;
    }

    validate()
    {
        throw "It should implement a validation."
    }

}
